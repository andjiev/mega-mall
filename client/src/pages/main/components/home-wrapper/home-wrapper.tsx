import React from 'react';
import styled from 'styled-components';

import { HomeWrapper } from './home-wrapper.styles';

interface WrapperProps {}

const MainHomeWrapper = (props: WrapperProps) => {
  return <HomeWrapper />;
};

export default MainHomeWrapper;
